{
  "name": "linhiq",
  "version": "0.1.0",
  "private": true,
  "description": "LinhIQ AI Learning Platform — Personalized AI Tutor for Cambridge IGCSE/A-Level",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "db:migrate": "pnpm --filter @linhiq/database db:migrate",
    "db:seed": "pnpm --filter @linhiq/database db:seed",
    "db:studio": "pnpm --filter @linhiq/database db:studio",
    "clean": "turbo clean && rimraf node_modules"
  },
  "dependencies": {
    "@prisma/client": "^6.4.0"
  },
  "devDependencies": {
    "prisma": "^6.4.0",
    "rimraf": "^6.0.0",
    "turbo": "^2.9.5"
  },
  "packageManager": "pnpm@10.7.1",
  "engines": {
    "node": ">=20.0.0"
  }
}
